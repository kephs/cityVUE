services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: cityvue
      POSTGRES_USER: cityvue
      POSTGRES_PASSWORD: cityvue_dev_only
    ports:
      - '127.0.0.1:5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U cityvue -d cityvue']
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - cityvue-postgres-data:/var/lib/postgresql/data

volumes:
  cityvue-postgres-data:
