# Database migrations

Migration files use `YYYYMMDDHHMMSS-kebab-case-description.ts` names. Phase A intentionally contains no migrations or business tables. Create a reviewed migration with `npm run migration:create -- description-in-kebab-case`.
