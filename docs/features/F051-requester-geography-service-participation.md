# F051 — Requester Geography & Service Participation Foundation

Status: implemented and validated as a development foundation. Starting checkpoint `a093cfa3ec635250b2f44128652a075dd5a4ab81`, clean synchronized main; verified personal development baseline: 26 applied migrations / zero pending, 12 requests, one synthetic Requester, two trusted links, tracking 1 active / 5 revoked. SR-202609-000010 remains anonymous and unlinked.

## Approved design

The user explicitly approved self-reported service-participation area, not residence; the question is **Which area do you associate with this service request? (Optional)**. Include Prefer not to say and explain that the answer neither changes nor derives from Service Location. Collection is voluntary, request-level and explicit. No coordinates, geometry, identity inference or permanent Requester profile. Anonymous PUBLIC requests may provide an area without Contact or identity linkage. INTERNAL collection is prohibited.

The dedicated `analytics.service_participation.read` permission has zero default grants and is excluded from broad bundles. Analytics additionally requires PUBLIC read and current Department/Division scope, before PostgreSQL aggregation. Permission alone never expands visibility. Development denial must precede a narrow explicit UAT grant.

Organization-scoped opaque area IDs, name, active state and deterministic order form the minimal catalog. Historical references survive rename/deactivation; analytics uses current labels. New selections require an active area. Request state is PROVIDED with area, DECLINED without area, or NOT_COLLECTED without area. Omitted input means not collected (including optional skips, unavailable collection and legacy callers); clients cannot explicitly submit NOT_COLLECTED. Existing requests receive only the truthful NOT_COLLECTED default. Request geography is immutable after creation. Finalized evidence retries return their original receipt before current area validation.

Analytics counts PUBLIC requests, never people. Server configuration defaults to threshold 5 and rejects values below 5. Positive counts below threshold become suppressed/null before disclosure; zero is shown as zero. Apply uniformly to areas, declined and not-collected. No grand totals, percentages, cross-tabs, drill-down or exports. Required inclusive UTC date-only ranges span 28–366 days, with no future dates; preview defaults to the last 365 days including today. Limited dimensions and minimum duration mitigate but do not eliminate repeated-query differencing. Safe required audit contains actor/Organization/period/threshold/correlation/time only; no counts or individual geography.

Individual geography stays out of staff detail/list, Contact, history, tracking, communication, attachment and search projections. No routing/assignment/eligibility effect. Existing geospatial and MapLibre resources remain Service Location tools only. Production area governance, analytics access/privacy/retention approval and administration remain deferred.

## Completion gates

Disposable migration apply/down/reapply and constraints; zero grants; authenticated authorization/scope and suppression tests; intake/retry/domain-separation and existing regressions; query-plan review; then target-verified development migration, minimal fictional areas and at most one new anonymous PUBLIC UAT request. Responsive light/dark/keyboard intake and protected analytics preview, logging/audit privacy, integrity comparison, temporary artifact cleanup, final docs/security review and local commit. No push, deployment or F052.

## Current validation and resumption checkpoint

Automated results: backend unit 237 passed; API E2E 40 passed; shared 64 passed; React 505 passed (35 files); full PostgreSQL suite 241 passed with zero skips. A subsequently added assisted-anonymous PROVIDED/DECLINED test passed with the complete affected request suite (197 passed, zero skips); the resulting combined PostgreSQL coverage is 242 cases. TypeScript, backend lint, server formatting and both builds passed; frontend retains the existing large-chunk build warning and has no configured lint script.

Disposable migration apply/down/reapply verified historical preservation and zero grants. Tests cover strict inputs, cross-Organization/inactive areas, immutable geography, anonymous/Contact-only/trusted identity separation, independent Service Location, mocked browser geolocation, EXIF/GPS stripping, finalized evidence retry and concurrency, required-audit rollback, current PUBLIC scope and suppression. EXPLAIN ANALYZE on the actual scoped grouping shape produced Aggregate/Sort/Nested Loop with index scans (one run used a sequential scan on small fixture data); grouping is in PostgreSQL, with no application request download or N+1. This is fixture-scale evidence, not a production benchmark.

The guarded personal development target was verified again before migration: localhost:5432, reqro_dev, reqro_dev_user, explicit development environment/profile. Migration applied successfully: 27 applied / zero pending. The fictional area CLI passed read-only dry-run then confirmation, creating three areas without request/grant changes. Verified 12 existing requests all NOT_COLLECTED and zero analytics grants. Existing-row hashes, tracking and private attachment files matched the pre-feature baseline before migration; repeat integrity verification after live UAT remains required.

The user confirmed HTTP 403 on the ungranted authenticated analytics endpoint before provisioning. The reviewed F036 dry-run and confirmation selected only the explicit analytics key on the previously selected personal principal. Both selected scopes (Public Works/Streets, Community Services/Parks) were already-active memberships and remain unchanged. Authorized preview, light/dark layouts at all five widths, keyboard period submission and invalid-period recovery passed. User confirmed normal API logs contain no discarded Contact draft, raw bodies, individual selections, hidden counts or bearer tokens. The required analytics audit has only safe actor/Organization/period/threshold/correlation/time metadata.

Live intake passed in both light/dark at 1440, 1280, 1024, 768 and 390 pixels, with visible selector focus and readable mobile label wrapping. Keyboard Tab from participation reaches the next attachment control. Changing Service Location preserved the selected area; choosing Prefer not to say preserved Service Location. Identified and declined Review drafts retained their choices through Back/Edit; the fictional Contact draft was erased by returning to anonymous before submission. No real device location was requested (automated mocked geolocation verifies separation).

Exactly one new retained UAT request is **SR-202609-000013**, internal ID `d174813a-b2bf-472b-a2c1-03bd9631d21a`, created through normal public intake. Safe database checks confirm anonymous, PROVIDED, zero Contact, null Requester link, one Location and one active default assignment. Integrity comparison after submission found all original rows unchanged, including grants/scopes, F049/F048 configuration and F050 identity/history. Expected additions: one request/location/answer/assignment, two operational events and two security Activity rows. Tracking and all four private files remain unchanged. No attachments were uploaded. Theme restored to light and temporary viewport reset; the temporary public-intake tab was closed, leaving the protected analytics tab ready.

Final integrity checks preserve every original domain row, tracking state and all private files. Only the explicit analytics permission and its owned role provenance changed among authorization records; no membership or scope changed. The [implementation report](F051-implementation-report.md) records the full evidence, retained fictional state and limitations. Exactly one F051 UAT request is retained; no additional request, tracking operation, push, deployment or F052 work is authorized.
