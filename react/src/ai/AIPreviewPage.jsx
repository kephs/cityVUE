import AIWorkspaceView from './AIWorkspaceView.jsx';

// Presentation only: no identity, repository, token, provider or execution dependency.
export default function AIPreviewPage() {
    return <AIWorkspaceView preview />;
}
